# Ignore all sub-dirs. More generic approach
# /*/
# Un-ignore some sub-dirs.
# !/env/

# However, instead, to ensure I keep my home dir organized, and ensure I'm aware of what's in it, I will ignore dirs selectively:
dl
fpco
notes
oss
pics
proj
.dotfiles-repo
.gnupg
.local
.mozilla
.cache
.config
.dotfiles-git
.ssh
.stack
.wget-hsts
.cargo
.ccache
.ghc
.mozbuild
.multirust
.nv
.pki
.rustup
.vim

# Questionable ignores
.gconf

# File ignores
.ICEauthority
.bash_history
.bash_logout
.sudo_as_admin_successful
.gitconfig
.hgrc
.viminfo
env/settings.sh

# Ignore emacs recovery files
.#*
