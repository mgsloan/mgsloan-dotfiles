# Queue the same service used at boot/resume; never wait for D-Bus in udev.
# SYSTEMD_WANTS only triggers on device activation, not every AC change.
SUBSYSTEM=="power_supply", KERNEL=="AC", ACTION=="change", \
RUN+="/usr/bin/systemctl --no-block start cpu-governor.service"
